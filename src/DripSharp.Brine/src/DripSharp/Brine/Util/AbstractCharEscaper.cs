// <auto-generated />
// Mechanically translated from: org/pkl/core/util/AbstractCharEscaper.java
// Upstream repository: https://github.com/apple/pkl.git
// Upstream revision: f7cac257ade5775c1dfc255f4fda2eacc296e9d0
// Translator: DripSharp 0.1.0
// IMPORTANT: This mechanically translated derivative has been changed from the upstream source.
// Applicable upstream notices: see NOTICE.txt.
#nullable enable
namespace DripSharp.Brine.Util;

internal abstract partial class AbstractCharEscaper
{
protected internal abstract string? FindReplacement(char ch);

public virtual string Escape(string str) {
var length = str.Length;
for (var i = 0; (i < length); i++) {
var ch = str[i];
if ((this.FindReplacement(ch)! != default!)) {
return this.DoEscape(str, i, new global::System.Text.StringBuilder((length * 2))).ToString();
}
}
return str;
}

public virtual void Escape(string str, global::System.Text.StringBuilder builder) {
this.DoEscape(str, 0, builder);
}

private global::System.Text.StringBuilder DoEscape(string str, int offset, global::System.Text.StringBuilder builder) {
var start = 0;
var length = str.Length;
for (var i = offset; (i < length); i++) {
var ch = str[i];
var replacement = this.FindReplacement(ch)!;
if ((replacement! == default!)) {
continue;
}
builder.Append(str, start, (i - start)).Append(replacement!);
start = (i + 1);
}
if ((start < length)) {
builder.Append(str, start, (length - start));
}
return builder;
}
}
