// <auto-generated />
// Mechanically translated from: org/pkl/core/util/StringUtils.java
// Upstream repository: https://github.com/apple/pkl.git
// Upstream revision: f7cac257ade5775c1dfc255f4fda2eacc296e9d0
// Translator: DripSharp 0.1.0
// IMPORTANT: This mechanically translated derivative has been changed from the upstream source.
// Applicable upstream notices: see NOTICE.txt.
#nullable enable
namespace DripSharp.Brine.Util;

internal sealed partial class StringUtils
{
private static readonly string TABLE = global::DripSharp.Runtime.JavaCompat.Concat(global::DripSharp.Runtime.JavaCompat.Concat(global::DripSharp.Runtime.JavaCompat.Concat("\u2002\u3000\r\u0085\u200A\u2005\u2000\u3000", "\u2029\u000B\u3000\u2008\u2003\u205F\u3000\u1680"), "\t \u2006\u2001\u202F\u00A0\f\u2009"), "\u3000\u2004\u3000\u3000\u2028\n\u2007\u3000");

private static readonly int MULTIPLIER = 1682554634;

private static readonly int SHIFT = global::DripSharp.Runtime.JavaCompat.IntLeadingZeros((StringUtils.TABLE.Length - 1));

public static bool IsWhitespace(char ch) {
return ((int)(StringUtils.TABLE[((StringUtils.MULTIPLIER * ch) >>> unchecked((int)(StringUtils.SHIFT)))]) == (int)(ch));
}

public static bool IsBlank(string str) {
for (var i = 0; (i < str.Length); i++) {
if (!(StringUtils.IsWhitespace(str[i]))) {
return false;
}
}
return true;
}

public static string TrimStart(string str) {
return str.Substring(StringUtils.IndexOfNonWhitespace(str));
}

public static string TrimEnd(string str) {
return global::DripSharp.Runtime.JavaCompat.StringSubstring(str, 0, StringUtils.LastIndexOfNonWhitespace(str));
}

public static void JoinToStringBuilder<T>(global::DripSharp.Brine.Util.AnsiStringBuilder builder, global::System.Collections.Generic.IEnumerable<T> coll, string delimiter, global::System.Action<T> eachFn) {
var i = 0;
foreach (var v in coll) {
if ((i != 0)) {
builder.Append(delimiter);
}
eachFn(v);
i++;
}
}

private static int IndexOfNonWhitespace(string str) {
var length = str.Length;
for (var i = 0; (i < length); i++) {
if (!(StringUtils.IsWhitespace(str[i]))) {
return i;
}
}
return length;
}

private static int LastIndexOfNonWhitespace(string str) {
var length = str.Length;
for (var i = length; (i > 0); i--) {
if (!(StringUtils.IsWhitespace(str[(i - 1)]))) {
return i;
}
}
return 0;
}
}
