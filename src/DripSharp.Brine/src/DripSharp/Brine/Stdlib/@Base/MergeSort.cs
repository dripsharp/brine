// <auto-generated />
// Mechanically translated from: org/pkl/core/stdlib/base/MergeSort.java
// Upstream repository: https://github.com/apple/pkl.git
// Upstream revision: f7cac257ade5775c1dfc255f4fda2eacc296e9d0
// Translator: DripSharp 0.1.0
// IMPORTANT: This mechanically translated derivative has been changed from the upstream source.
// Applicable upstream notices: see NOTICE.txt.
#nullable enable
namespace DripSharp.Brine.Stdlib.@Base;

internal sealed partial class MergeSort
{
private static readonly int INITIAL_MERGE_SORT_STRIDE = 8;

public static object[] Sort(global::DripSharp.Brine.Runtime.Truffle.api.frame.VirtualFrame frame, object[] array, global::DripSharp.Brine.Stdlib.@Base.CollectionNodes.SortComparatorNode comparator, global::DripSharp.Brine.Runtime.VmFunction? function) {
var length = array.Length;
var temp = new object[length];
for (var start__40_14 = 0; (start__40_14 < length); start__40_14 += MergeSort.INITIAL_MERGE_SORT_STRIDE) {
var end__41_11 = global::System.Math.Min((start__40_14 + MergeSort.INITIAL_MERGE_SORT_STRIDE), length);
MergeSort.InsertionSort(frame, array, start__40_14, end__41_11, comparator, function!);
}
for (var stride = MergeSort.INITIAL_MERGE_SORT_STRIDE; (stride < length); stride *= 2) {
for (var start__46_16 = 0; (start__46_16 < (length - stride)); start__46_16 += (stride + stride)) {
var end__47_13 = global::System.Math.Min(((start__46_16 + stride) + stride), length);
var mid = (start__46_16 + stride);
MergeSort.Merge(frame, array, temp, start__46_16, mid, end__47_13, comparator, function!);
}
}
return array;
}

private static void Merge(global::DripSharp.Brine.Runtime.Truffle.api.frame.VirtualFrame frame, object[] array, object[] temp, int start, int mid, int end, global::DripSharp.Brine.Stdlib.@Base.CollectionNodes.SortComparatorNode comparator, global::DripSharp.Brine.Runtime.VmFunction? function) {
if (comparator.ExecuteWith(frame, array[(mid - 1)], array[mid], function!)) {
return;
}
global::DripSharp.Runtime.JavaCompat.ArrayCopy(array, start, temp, start, (end - start));
var i = start;
var j = mid;
for (var k = start; (k < end); k++) {
if ((i >= mid)) {
array[k] = temp[j++];
} else {
if ((j >= end)) {
array[k] = temp[i++];
} else {
if (comparator.ExecuteWith(frame, temp[j], temp[i], function!)) {
array[k] = temp[j++];
} else {
array[k] = temp[i++];
}
}
}
}
}

private static void InsertionSort(global::DripSharp.Brine.Runtime.Truffle.api.frame.VirtualFrame frame, object[] array, int start, int end, global::DripSharp.Brine.Stdlib.@Base.CollectionNodes.SortComparatorNode comparator, global::DripSharp.Brine.Runtime.VmFunction? function) {
for (var i = start; (i < end); i++) {
for (var j = i; ((j > start) && comparator.ExecuteWith(frame, array[j], array[(j - 1)], function!)); j--) {
object swap = array[j];
array[j] = array[(j - 1)];
array[(j - 1)] = swap;
}
}
}
}
