// <auto-generated />
// Mechanically translated from: org/pkl/core/messaging/AbstractMessagePackEncoder.java
// Upstream repository: https://github.com/apple/pkl.git
// Upstream revision: f7cac257ade5775c1dfc255f4fda2eacc296e9d0
// Translator: DripSharp 0.1.0
// IMPORTANT: This mechanically translated derivative has been changed from the upstream source.
// Applicable upstream notices: see NOTICE.txt.
#nullable enable
namespace DripSharp.Brine.Messaging;

internal abstract partial class AbstractMessagePackEncoder : global::DripSharp.Brine.Messaging.MessageEncoder
{
protected internal readonly global::DripSharp.Brine.Runtime.ExcludedMessagePackPacker packer = default!;

public AbstractMessagePackEncoder(global::DripSharp.Brine.Runtime.ExcludedMessagePackPacker packer) {
this.packer = packer;
}

protected internal abstract void EncodeMessage(global::DripSharp.Brine.Messaging.Message msg);

public void Encode(global::DripSharp.Brine.Messaging.Message msg) {
lock (this.packer) {
(this.packer).PackArrayHeader(2);
(this.packer).PackInt(msg.CreateType().GetCode());
this.EncodeMessage(msg);
(this.packer).Flush();
}
}

protected internal virtual void PackMapHeader(int size, object? value1) {
(this.packer).PackMapHeader((size + ((value1! != default!) ? 1 : 0)));
}

protected internal virtual void PackMapHeader(int size, object? value1, object? value2) {
(this.packer).PackMapHeader(((size + ((value1! != default!) ? 1 : 0)) + ((value2! != default!) ? 1 : 0)));
}

protected internal virtual void PackKeyValue(string name, long? value) {
if ((value! == default!)) {
return;
}
(this.packer).PackString(name);
(this.packer).PackLong((long)(value!));
}

protected internal virtual void PackKeyValue(string name, string? value) {
if ((value! == default!)) {
return;
}
(this.packer).PackString(name);
(this.packer).PackString(value!);
}

protected internal virtual void PackKeyValue(string name, sbyte[] value) {
if ((value == default!)) {
return;
}
(this.packer).PackString(name);
(this.packer).PackBinaryHeader(value.Length);
(this.packer).WritePayload(value);
}

protected internal virtual void PackKeyValue(string name, bool value) {
(this.packer).PackString(name);
(this.packer).PackBoolean(value);
}
}
